# Examples

This directory contains runnable examples for `easyhash`.

## Available Examples

- `password/main.go`: shows the recommended high-level `Hash` / `Verify` path and the low-level compatibility helpers that still exist for legacy integrations.

Run an example with:

```bash
go run ./examples/password
```
